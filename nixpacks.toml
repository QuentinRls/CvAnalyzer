[phases.setup]
nixPkgs = ["nodejs_18", "python39"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd frontend && npm ci"
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "python main.py"
